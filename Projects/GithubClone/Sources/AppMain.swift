import SwiftUI

@main
struct AppMain: App {
  var body: some Scene {
    WindowGroup {
      VStack {
        Spacer()
        Text("AppMain")
          .foregroundStyle(Color.blue)
        Spacer()
      }
    }
  }
}
